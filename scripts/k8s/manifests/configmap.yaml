apiVersion: v1
kind: ConfigMap
metadata:
  name: xclaw-config
  labels:
    app: xclaw
data:
  xclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.xclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "XClaw Assistant",
            "workspace": "~/.xclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # XClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
