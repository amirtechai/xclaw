apiVersion: v1
kind: Service
metadata:
  name: xclaw
  labels:
    app: xclaw
spec:
  type: ClusterIP
  selector:
    app: xclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
