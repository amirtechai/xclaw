---
summary: "CLI reference for `xclaw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `xclaw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `xclaw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `xclaw clawbot qr` (same behavior as [`xclaw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `xclaw clawbot qr` -> `xclaw qr`
